server:
  port: 8080
spring:
  config:
    import: optional:file:.env[.properties]
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb://localhost:27017/trifusion26}
  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 5MB
jwt:
  secret: ${JWT_SECRET:trifusion26_default_jwt_secret_key_at_least_32_chars}
  expiration: 86400000
admin:
  email: ${ADMIN_EMAIL:svhectrifusion2026@gmail.com}
  password: ${ADMIN_PASSWORD:svhec@7325}
cloudinary:
  cloud-name: ${CLOUDINARY_CLOUD_NAME:placeholder}
  api-key: ${CLOUDINARY_API_KEY:placeholder}
  api-secret: ${CLOUDINARY_API_SECRET:placeholder}
cors:
  allowed-origin: ${CORS_ORIGIN:http://localhost:5173}
